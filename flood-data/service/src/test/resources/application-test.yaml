spring:
  application:
    name: Flood API Test
  datasource:
    url: jdbc:sqlite:flood-data/service/src/main/resources/db/flood.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        globally_quoted_identifiers: true